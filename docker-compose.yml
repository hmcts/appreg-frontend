services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: appreg-frontend:latest
    container_name: appreg-frontend
    environment:
      NODE_ENV: production
      PORT: 4000
    ports:
      - "4000:4000"
    restart: unless-stopped
